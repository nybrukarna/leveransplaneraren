<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>Leveransplaneraren - Nybrukarna</title>

    <!-- Bootstrap -->
    <%= stylesheet_link_tag('bootstrap.min') %>
    <%= stylesheet_link_tag('application') %>

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
      <div class="navbar navbar-default navbar-fixed-top">
        <div class="container">
        <div class="navbar-header">
          <!--
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          </button>
          -->
          <a class="navbar-brand" href="/"><img src="/images/logo.png" height="20"></a>
        </div>
          <!--
        <div id="bs-example-navbar-collapse-1" class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li class="active"><a href="/">Home</a></li>
            <li><a href="/">Contact</a></li>
          </ul>
        </div>
          -->
          <% unless session[:current_user].blank? %>
            <a class="btn btn-default navbar-btn navbar-right" href="/session/destroy">Logga ut</a>
            <p class="navbar-text navbar-right"> Inloggad som <b><%= session[:user][:name] %> (<%= session[:user][:email] %>)</b>&nbsp;&nbsp;</p>
          <% end %>
        </div>
      </div>
    <div class="container">
      <% if flash[:info] %>
      <div class="alert alert-info alert-dismissible" role="alert">
          <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <strong>Info!</strong> <%= flash[:info] %>
      </div>
      <% end %>
      <% if flash[:success] %>
      <div class="alert alert-success alert-dismissible" role="alert">
          <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <%= flash[:success] %>
      </div>
      <% end %>
      <% if flash[:alert] %>
      <div class="alert alert-danger alert-dismissible" role="alert">
          <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <strong>Något är fel!</strong> <%= flash[:alert] %>
      </div>
      <% end %>
    <%= yield %>
    </div>
    <div class="container footer">
      Nybrukarna, <a href="mailto:info@nybrukarna.se">info@nybrukarna.se</a>, Copyright <%= Time.now.year %>
    </div>

    <%= javascript_include_tag('jquery.min') %>
    <%= javascript_include_tag('bootstrap.min') %>
  </body>
</html>
